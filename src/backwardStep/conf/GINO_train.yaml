defaults:
  - _self_
  - common_train

encoder: GINO
dir_weights: ./${encoder}/weights/
dir_outputs: ./${encoder}/outputs/
start_epoch: 0
epochs: 500

# Encoder Parameters
gno_transform_type: nonlinear

# Network string
network_str: ${input_sampling_rate}_${gno_transform_type}