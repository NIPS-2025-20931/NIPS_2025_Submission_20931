defaults:
  - _self_
  - common_test

encoder: PointNetPP
dir_weights: ./${encoder}/weights/
dir_outputs: ./${encoder}/outputs/
start_epoch: 500
epochs: 500

# Encoder Parameters
sampling_method: FPS
latent_feats: 128

# Network string
network_str: ${sampling_method}